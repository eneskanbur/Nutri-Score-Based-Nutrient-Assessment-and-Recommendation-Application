<resources>
    xmlns:tools="http://schemas.android.com/tools"
    <!-- Base application theme. -->
    <style name="Base.Theme.NutriScoreBasedApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NutriScoreBasedApplication" parent="Base.Theme.NutriScoreBasedApplication" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/background</item> <!-- Yeni test logo -->
        <item name="windowSplashScreenAnimationDuration">200</item> <!-- Daha kısa süre -->
        <item name="postSplashScreenTheme">@style/Theme.NutriScoreBasedApplication</item> <!-- Tam tema yolu -->
    </style>
</resources>